# Sample GPT app to play with the API

## Install packages
`
npm i
`

Set environment variables for gpt key:
GPT_API_KEY=

## Serve express API locally
`
npm run start:api
`

## Serve UI locally
`
npm run start:client
`